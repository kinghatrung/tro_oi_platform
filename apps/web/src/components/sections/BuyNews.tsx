import { SectionNews } from './SectionNews';

/** Displays the latest property sale listings. */
export function BuyNews() {
  return <SectionNews title="Tin mua bán mới đăng" buttonText="Xem thêm tin mua bán" />;
}
