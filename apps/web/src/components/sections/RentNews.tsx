import { SectionNews } from './SectionNews';

/** Displays the latest property rental listings. */
export function RentNews() {
  return <SectionNews title="Tin cho thuê mới đăng" buttonText="Xem thêm tin cho thuê" />;
}
